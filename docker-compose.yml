version: "3.9"
services:
  web:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: node server.js
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
